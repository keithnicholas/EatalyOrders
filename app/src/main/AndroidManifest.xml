<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.ubco.cosc341.eatalyorders">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PendingOrders" />
        <activity android:name=".Reservations" />
        <activity android:name=".Menu" />
        <activity android:name=".Booking" />
        <activity android:name=".ResConfirm" />
        <activity android:name=".OrderSummary" />
        <activity android:name=".OrderConfirm" />
        <activity android:name=".PickupOrder" />
        <activity android:name=".DeliveryOrder"></activity>
    </application>

</manifest>